import styled from "styled-components";

const HeaderWrapper = styled.header`
  .row {
    display: flex;
    align-items: center;
  }
  input {
    width: 100%;
  }
`;

export default HeaderWrapper;
